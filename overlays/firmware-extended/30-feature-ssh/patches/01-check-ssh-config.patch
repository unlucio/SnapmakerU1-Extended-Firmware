--- a/etc/init.d/S50dropbear
+++ b/etc/init.d/S50dropbear
@@ -14,7 +14,15 @@ log()
 	echo "$1"
 }
 
+EXTENDED_CFG="/home/lava/printer_data/config/extended/extended2.cfg"
+SSH_ENABLED=$(/usr/local/bin/extended-config.py get "$EXTENDED_CFG" remote_access ssh false)
+
 start() {
+	if [ "$SSH_ENABLED" != "true" ]; then
+		echo "SSH is disabled in extended2.cfg, not starting."
+		return
+	fi
+
 	DROPBEAR_ARGS="$DROPBEAR_ARGS -R"
 
 	# If /etc/dropbear is a symlink to /var/run/dropbear, and
