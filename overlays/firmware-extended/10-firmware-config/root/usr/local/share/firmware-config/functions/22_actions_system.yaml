actions:
  system:
    items:
      reboot-system:
        label: Reboot System
        description: Reboot the printer system.
        confirm: true
        cmd:
          - /sbin/reboot
        background: true
        message: "Rebooting system..."
